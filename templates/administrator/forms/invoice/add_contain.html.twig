<div id="custom-content" class="white-popup-block white-popup-block-padding">
    <div class="_lightbox-std">
        <div class="content-lightbox">
            <div class="center">
                <h2 class="text-center">AJOUTER DU CONTENU</h2>
                {{ form_start(add_contain_form, {'attr' : {'class': 'form-std', 'autocomplete': 'off'}} ) }}
                <fieldset>
                    <div class="input-rental">
                        {% include 'administrator/partials/forms/_new_prestation.html.twig' %}
                        <br />
                        {% include 'administrator/partials/forms/_new_services.html.twig' %}
                        <br />
                        {% include 'administrator/partials/forms/_new_materials.html.twig' %}
                    </div>
                </fieldset>
                <div class="input-submit text-center">
                    <input type="submit" value="Valider"/>
                </div>
                <input type="hidden" name="calendar_form[user]" value="{{ user.id }}">
                <input type="hidden" name="calendar_form[invoice]" value="{{ invoice.id }}">
                {{ form_end(add_contain_form) }}
            </div>
        </div>
    </div>
</div>